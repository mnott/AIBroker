# Written automatically by `aibroker ota up`.
# UID/GID must match the host user so bind-mount writes succeed.
UID=
GID=
# Comma-separated Tailscale login emails allowed to POST/DELETE.
# Example: mnott@github,mnott@
AIBROKER_OTA_PUBLISHERS=
